# 文生视频_原图插入 V2.0 配置信息与回滚手册

> **归档版本**：V2.0（对应 Git Tag `v1.2.3` / 实验 A1 优化升级版）  
> **归档日期**：2026-09-11  
> **配置定位**：数字人 832x480 电影画幅 + 首帧母本硬替换 + **关闭潜空间时序软锚定 (anchor_weight = 0)** 增强版。用户评定“比上一个版本更好”，彻底消除了潜空间强制混合对 DiT 流场的干扰，微表情更加自然通透。

---

## 一、 与 V1.0 核心变更对比

| 对比维度 | V1.0 基线配置 (v1.2.2) | **V2.0 优化升级配置 (v1.2.3 / 实验 A1)** | 设计原因与物理增益 |
| :--- | :--- | :--- | :--- |
| **Slice 0 (第0帧)** | 100% 锁定首帧加噪基准，解码前直接替换 | **100% 锁定首帧加噪基准，解码前直接替换** | 严格维持首帧与原图 1:1 绝对像素级对齐 |
| **Slice 1 (第1~4帧)** | 85% 强阻尼加噪软锚定 (`anchor_weight = 0.85`) | **完全跳过锚定融合 (`anchor_weight = 0.0`)** | 消除潜空间强制线性混合对 DiT 连续速度流场的干扰 |
| **Slice 2 (第5~8帧)** | 80% 强阻尼加噪软锚定 (`anchor_weight = 0.80`) | **完全跳过锚定融合 (`anchor_weight = 0.0`)** | 彻底放开潜空间动作自由度，杜绝加噪混合引入的高频失真 |
| **去噪模式** | 首帧广播 + 潜空间每步阻尼拉扯 | **首帧先验噪声空间自由去噪** | 面部光影过度更连贯，眼神更加从容自然 |
| **诊断与监控** | 基础计时与显存监控 | **全链路 14 项底层运行时诊断探针** | 纳秒级记录 timesteps、sigmas、VAE 编解码次数等完整指标 |

---

## 二、 核心架构与模型组件

| 模块 | 模型全称 / 来源 | 精度 / 规格 | 运行设备 | 内存/显存驻留策略 |
| :--- | :--- | :--- | :--- | :--- |
| **主干生成模型** | Wan2.1-T2V-1.3B (Diffusion Transformer) | FP16 | GPU (CUDA) | 独占显存，峰值 2952.19MB，去噪完成后彻底释放 |
| **文本编码器** | Google UMT5-XXL | FP8 (Float8) | 系统内存 (CPU) | 纯 CPU 内存推理，完全不占用 4GB GPU 显存 |
| **分词器** | Wan2.1 AutoTokenizer | 官方分词词表 | CPU | 轻量驻留 |
| **调度器** | FlowMatchEulerDiscreteScheduler | 欧拉连续流匹配 | CPU / GPU | shift: 1.0, prediction_type: flow_prediction |
| **视频编解码器** | AutoencoderKLWan (3D Causal VAE) | **全精度 FP32** | GPU (CUDA) | 解耦至阶段4单独载入，开启 Tiling/Slicing，解码后释放 |

---

## 三、 核心生成参数与超参数清单

```json
{
  "version": "2.0",
  "pipeline_name": "WanT2VLowVramPipeline",
  "resolution": {
    "width": 832,
    "height": 480,
    "aspect_ratio": "16:9"
  },
  "temporal": {
    "num_frames": 9,
    "target_num_frames": 8,
    "fps": 8,
    "duration_sec": 1.0
  },
  "inference": {
    "num_inference_steps": 20,
    "strength": 0.20,
    "effective_steps": 4,
    "guidance_scale": 2.0,
    "seed": 42
  },
  "temporal_anchoring": {
    "enable_temporal_anchoring": false,
    "slice_0_weight": 1.0,
    "slice_1_weight": 0.0,
    "slice_2_weight": 0.0
  },
  "post_processing": {
    "temporal_contrast_restoration": true,
    "temporal_contrast_ratio": 0.85,
    "adaptive_edge_filter": true,
    "edge_filter_threshold": 8.0
  },
  "hardware_limits": {
    "vram_limit_gb": 3.6,
    "peak_vram_mb": 2952.19
  }
}
```

---

## 四、 14 项底层运行时诊断数据快照

```text
1. scheduler 实际类名: FlowMatchEulerDiscreteScheduler
2. scheduler.config 完整关键参数:
   - shift: 1.0
   - prediction_type: flow_prediction
   - num_train_timesteps: 1000
3. num_inference_steps 配置值: 20
4. 实际执行的 scheduler timestep 数量: len(timesteps) = 4
5. 实际 timesteps 前后 5 个值: [158.7368, 106.1579, 53.5789, 1.0]
6. sigmas 取值:
   - 前5个: [1.0, 0.9474, 0.8948, 0.8423, 0.7897]
   - 后5个: [0.1587, 0.1062, 0.0536, 0.001, 0.0]
7. guidance_scale 实际值: 2.0
8. strength 实际值: 0.20
9. VAE encode 调用总次数: 1 次 (首帧离线编码)
10. VAE decode 调用总次数: 1 次 (分镜批量解码)
11. 每个 temporal slice 实际 frame 范围:
    - Slice 0: Frame 0 (第0帧)
    - Slice 1: Frame 1 ~ Frame 4 (第1~4帧)
    - Slice 2: Frame 5 ~ Frame 8 (第5~8帧)
12. 每个 slice 使用的 anchor 权重:
    - Slice 0: 1.0 (原图直接替换)
    - Slice 1: 0.0 (anchor_weight = 0，关闭锚定)
    - Slice 2: 0.0 (anchor_weight = 0，关闭锚定)
13. 是否存在 generated frame -> VAE encode 参与后续生成: 否 (False)
14. 是否存在 clean reference 直接与当前 noisy latent 线性混合: 否 (False)
```

---

## 五、 一键回滚 / 复现执行命令

若后续探索出现效果劣化，可在项目根目录下执行以下命令，严格 100% 还原当前 V2.0 最优画质与全部 8 帧输出：

```bash
.venv\Scripts\python run_avatar_pipeline.py --shot shot_01 --width 832 --height 480 --steps 20 --strength 0.20 --seed 42 --no-temporal-anchor --output-dir run_A1 --force
```
