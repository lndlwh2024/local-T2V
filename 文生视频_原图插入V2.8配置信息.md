# 文生视频数字人原图插入技术方案 V2.8 配置信息 (阶段 D1.5: 动作响应能力审计与全序列架构边界裁定)

## 一、版本演进说明

- **V1.0 基线版**：首帧单切片注入 + 软锚定（85%/80%）+ 20 步。重影与水波纹严重。
- **V2.0 ~ V2.3 隔离验证版**：彻底移除时序软锚定（根除波纹），关闭后处理发现内生发灰，通过纯 VAE 隔离测试确诊 VAE 高保真（衰减仅 0.16%）。
- **V2.4 (B3 黄金基线)**：接入全时序参考潜变量一次性因果编码（$T=3$），黑位完全零上浮（$\Delta = -0.80$），动态范围零衰减（$\Delta = -0.32$），彻底根治泛白发雾。
- **V2.5 (C1 验证版)**：`strength = 0.25`（12 步去噪），微动态初步激活，零波纹零发雾。
- **V2.6 (阶段 C 生产模式甜点搜索裁定版)**：
  - 完成阶段 C 微动作梯度探索（B3: 0.20 -> C1: 0.25 -> C2: 0.30 -> C3: 0.35）；
  - 正式将 **C2 (`strength = 0.30`) 确立并封板为生产黄金甜点版**（Production Sweet Spot / Golden Baseline）；
  - 正式将 **C3 (`strength = 0.35`) 归档为上边界压力参考版**（Experimental Upper Boundary），明确不作为生产默认参数。
- **V2.7 (阶段 D1 大动作崩裂初探版)**：
  - 在 C2 封板参数（`strength = 0.30`, 15 步）下注入 35°~45° 大转头 Prompt；
  - 初步发现画质极度稳定、零波纹零崩裂，但大幅动作未生效。
- **V2.8 (阶段 D1.5 动作响应能力深度审计与架构边界裁定版，本版本)**：
  - 实施 D1.5-A (`strength = 0.35`, 17 步) 与 D1.5-B (`strength = 0.40`, 20 步) 梯度压力测试；
  - 量化确认：即使强度提升至 0.40，平均帧间像素位移仅由 1.47 蠕动至 1.65，转头幅度依然为 0°；
  - **核心判定下达**：“Full-Sequence static reference 架构天然适合微动作，不适合纯文本驱动的大动作；问题不属于模型 bug，而属于架构边界。”

---

## 二、阶段 D1.5 动作响应梯度实验对照全景

> **标准光度学亮度公式**：$Y = 0.299 \times R + 0.587 \times G + 0.114 \times B$

| 实验编号 | Strength | 实际去噪步数 | Prompt 动作意图 | 实测转头趋势 | 帧间位移 (f1 vs f7 像素均值) | Frame 7 黑位 | 黑位漂移 (Delta) | 动态范围衰减率 | 画面可用性与状态 |
| :--- | :---: | :---: | :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **D1** | 0.30 | 15 步 | 35°~45° 左偏转头 | **无 (0°)** | 1.4736 | 4.631 | +1.299 | 1.05% | 画质极佳，微动作稳定 |
| **D1.5-A** | 0.35 | 17 步 | 35°~45° 左偏转头 | **无 (0°)** | 1.5433 | 4.745 | +1.598 | 1.09% | 画质良好，动作几乎未动 |
| **D1.5-B** | 0.40 | 20 步 | 35°~45° 左偏转头 | **无 (0°)** | 1.6516 | 5.859 | +2.484 | 1.81% | 仍无转头，暗部开始微抬 |

---

## 三、逐级明确评价

### 1. D1.5-A (`strength = 0.35`, 17 步) 评价
- **动作是否显著增强**：**【否】**。实测 f1 vs f7 平均像素差为 `1.5433`，仅比 D1 (0.30) 的 `1.4736` 增加 `0.07`，人眼观察几乎完全静止。
- **是否终于出现 35°~45° 转头趋势**：**【完全未出现】**。人物面部轮廓、鼻尖中线与双耳朝向保持 100% 正面，转角为 0°。
- **是否开始出现波纹/灰化/身份漂移**：
  - 波纹：无（0%）；
  - 灰化：极轻微（黑位漂移量仅 `+1.598`，动态范围衰减 1.09%）；
  - 身份漂移：无（五官面容与原图完全一致）。

### 2. D1.5-B (`strength = 0.40`, 20 步) 评价
- **动作是否显著增强**：**【否】**。实测 f1 vs f7 平均像素差为 `1.6516`，相比 0.30 仅微弱上浮 `0.18`，几何大动作依然未被触发。
- **是否终于出现 35°~45° 转头趋势**：**【完全未出现】**。即使去噪迭代增加到 20 步（占总步数 40%），正面参考潜变量依然牢牢统治时空序列。
- **是否开始出现波纹/灰化/身份漂移**：
  - 波纹与重影：依然未发生；
  - 灰化与黑位：开始出现可感知的暗部上浮（Frame 7 黑位达 `5.859`，黑位漂移 `+2.484`，动态范围衰减达 `1.81%`）；
  - 身份与结构：脸型骨相依然稳定，但高去噪步数使得皮肤微纹理产生轻微去特征柔化。

---

## 四、最终裁定与机理本质

### 1. 最终技术判定
> **“Full-Sequence static reference 架构天然适合微动作，不适合纯文本驱动的大动作；问题不属于模型 bug，而属于架构边界。”**

### 2. 深度机理溯源
1. **时空拓扑因果约束（Causal Temporal Rigidity）**：
   在 Full-Sequence Reference 架构中，我们将 9 帧相同的正面原图输入 3D Causal VAE，编码为 $T=3$ 的潜变量张量 `[1, 16, 3, 60, 104]`。该潜变量张量天然在时间轴上编码了“帧间完全静态不变”的时序特征。
2. **扩散去噪反转门槛过高（Denoising Inversion Barrier）**：
   要实现 35°~45° 转头，意味着画面必须在空间上暴露全新的后脑勺、耳朵内侧、下颌侧翼，并彻底重构背景遮挡关系。在 strength $\le 0.40$ 下，初始潜变量中正面人脸的信息占据主导；纯文本 Cross-Attention 的引导向量在没有显式几何控制信息（如 Pose Keypoints / Depth Map）的辅助下，根本无法推翻全时序静态张量的拓扑惯性。
3. **架构的“防崩裂”特质反向构成了“动作阻尼”**：
   这一架构的巨大工程优势在于：**无论提示词多么激烈失控，画面都不会发生扭曲撕裂、鬼影或波纹，而是自适应滤波退化为安全的正面微动态**；但其对应的物理边界即是**“无法由文本凭空驱动大动作”**。

---

## 五、工程结论与后续技术路线建议

1. **生产封板维持 C2**：
   正式生产环境继续严格锁定 **C2 (`strength = 0.30`, 15 步)** 作为口播微动作黄金基线（`AVATAR_STABLE_MICRO_MOTION`），严禁在生产中盲目调高 strength 到 0.40。
2. **大动作需求的技术解法（非调参解法）**：
   如果后续业务必须实现大幅转身、转头等动作，应采用正确的架构进阶路线：
   - **方案 A (多视角原图/关键帧插值)**：首帧提供正面原图，末帧或转折帧提供 45° 侧面原图，在潜空间做时序插值引导；
   - **方案 B (显式几何控制网 ControlNet/DWPose)**：引入时序骨架控制流引导 DiT 在去噪过程中发生几何偏转；
   - **方案 C (动作驱动视频管线)**：利用专门的 LivePortrait 或类似头部姿态驱动层对生成潜变量施加姿态变换。
