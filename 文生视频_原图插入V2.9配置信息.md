# 文生视频数字人原图插入技术方案 V2.9 配置信息 (阶段 D2-Control: 纯 T2V 大动作响应对照与根因裁定)

## 一、版本演进说明

- **V1.0 基线版**：首帧单切片注入 + 软锚定（85%/80%）+ 20 步。重影与水波纹严重。
- **V2.0 ~ V2.3 隔离验证版**：彻底移除时序软锚定（根除波纹），关闭后处理发现内生发灰，通过纯 VAE 隔离测试确诊 VAE 高保真（衰减仅 0.16%）。
- **V2.4 (B3 黄金基线)**：接入全时序参考潜变量一次性因果编码（$T=3$），黑位完全零上浮（$\Delta = -0.80$），动态范围零衰减（$\Delta = -0.32$），彻底根治泛白发雾。
- **V2.5 (C1 验证版)**：`strength = 0.25`（12 步去噪），微动态初步激活，零波纹零发雾。
- **V2.6 (阶段 C 生产模式甜点搜索裁定版)**：
  - 完成阶段 C 微动作梯度探索（B3: 0.20 -> C1: 0.25 -> C2: 0.30 -> C3: 0.35）；
  - 正式将 **C2 (`strength = 0.30`) 确立并封板为生产黄金甜点版**（Production Sweet Spot / Golden Baseline）；
  - 正式将 **C3 (`strength = 0.35`) 归档为上边界压力参考版**（Experimental Upper Boundary），明确不作为生产默认参数。
- **V2.7 ~ V2.8 (阶段 D1 / D1.5 动作响应能力审计版)**：
  - 在全序列参考架构下进行 strength 0.30 / 0.35 / 0.40 压力测试；
  - 确认在静态全序列参考下，大转头动作完全被抑制（位移仅 1.47~1.65，转角为 0°）。
- **V2.9 (阶段 D2-Control 纯 T2V 对照与根因裁定版，本版本)**：
  - 彻底关闭全时序参考 (`use_full_sequence_reference = False`)，恢复纯随机高斯噪声初始化 (`initial_latents_source = RANDOM_NOISE`)；
  - 运行原生 50 步纯 T2V 去噪（`actual_iteration_count = 50`, `CFG = 2.0`）；
  - **根本诱因正式确立 (判定 A 生效)**：纯 T2V 下模型准确响应大动作 Prompt 生成了 45°~60° 的侧转侧脸姿态，正式确认 **Full-Sequence Static Reference 是大动作被锁死的根本原因**；
  - **C2/B3 架构永久限定为：`AVATAR_STABLE_MICRO_MOTION`**。

---

## 二、实验 D2-Control 核心配置与执行参数

### 1. 控制变量清单
- **模型架构**：Wan2.1-T2V-1.3B (Transformer FP16 + UMT5-XXL FP8 + FP32 3D Causal VAE)
- **参考潜变量初始化模式**：`use_full_sequence_reference = False`
- **初始潜变量来源**：`initial_latents_source = RANDOM_NOISE` (纯随机高斯噪声，零参考图输入)
- **时序软锚定**：`enable_temporal_anchoring = False` (`anchor_weight = 0.0`)
- **后处理状态**：`enable_post_processing = False` (纯净 Raw Decoded 帧导出)
- **去噪参数**：`num_inference_steps = 50`, `actual_iteration_count = 50`, `guidance_scale = 2.0`, `seed = 42`
- **Scheduler**：`FlowMatchEulerDiscreteScheduler (shift=1.0)`
- **画幅规格**：832x480 @ 8fps, 9 帧生成输出前 8 帧 (1.0 秒)
- **正向 Prompt**：
  `A photorealistic mature Asian man with a natural very short shaved buzz-cut hairstyle, groomed salt-and-pepper goatee, wearing a plain white crewneck t-shirt, standing in a modern high-tech broadcast studio with soft ambient studio lighting. Over a 1-second shot, starting from a frontal pose, he performs a clearly larger natural motion: he gradually turns his head and upper torso about 35 to 45 degrees to his left, with visible shoulder movement, ending in the clearly turned pose. Natural continuous motion, clear head rotation, clear upper-body rotation, visible change in facial perspective.`
- **Prompt Embedding SHA256 Checksum**：
  `388545186af5c62b4186acf56639bf9e85df93a1433ec3d1667a2e9039895e5f`
- **负向 Prompt**：
  `static pose, frozen pose, no motion, repeated identical frames, ghosting, double image, distorted face`

---

## 三、实验结果与横向全景对比

> **标准光度学亮度公式**：$Y = 0.299 \times R + 0.587 \times G + 0.114 \times B$

| 实验版本 | 初始潜变量来源 | Strength / 步数 | 实测人脸朝向姿态 | 帧间位移 (f1 vs f7 像素差) | Frame 7 黑位 | 黑位漂移 (Delta) | 动态范围衰减率 | 状态评价与定位 |
| :--- | :--- | :---: | :---: | :---: | :---: | :---: | :---: | :--- |
| **C2 (生产甜点)** | 9帧静态原图因果母本 | 0.30 / 15步 | **纯正面 (0°)** | 1.7421 | 1.837 | +0.854 | 0.86% | **★★★★★ 正式生产封板** |
| **D1 (大动作压力)** | 9帧静态原图因果母本 | 0.30 / 15步 | **纯正面 (0°)** | 1.4736 | 4.631 | +1.299 | 1.05% | 画质极佳，动作被锁死 |
| **D1.5-B (极限压力)**| 9帧静态原图因果母本 | 0.40 / 20步 | **纯正面 (0°)** | 1.6516 | 5.859 | +2.484 | 1.81% | 画质微退，动作仍锁死 |
| **D2-Control (本轮对照)**| **纯随机噪声 (RANDOM_NOISE)** | **1.00 / 50步** | **大角度侧转 (~45°~60°)** | **16.4894** | **41.407** | **+15.766** | **21.20%** | **动作响应成功 / 灰化严重** |

---

## 四、最终裁定与根因剖析

### 1. 最终技术裁定 (判定 A 正式生效)
> **“纯 T2V 模式下模型准确响应了大动作 Prompt 并生成了 45°~60° 的清晰侧转侧脸姿态；由此正式确诊：Full-Sequence Static Reference 是大动作被锁死的根本原因。C2/B3 架构永久限定为：AVATAR_STABLE_MICRO_MOTION，大动作生产必须使用另一条 T2V / 独立架构。”**

### 2. 双向机理印证
1. **为什么 Full-Sequence Reference 会锁死大动作？**
   - 9 帧相同的正面原图经 3D Causal VAE 编码后，构造了一个在时间切片 $T=3$ 上全局均质的正面人脸潜变量空间；
   - 在扩散去噪过程中，即使文本 Cross-Attention 注入了“转头 45°”的语义，初始潜变量强大的正面拓扑惯性构成了极高的能量壁垒，阻断了模型对耳后、侧脸和背景遮挡的重构；
   - 模型在去噪步数受限（15~20 步）时，选择“吸收提示词为微动”，从而表现出绝对锁死。
2. **纯 T2V 为什么能转头？同时为什么不能直接用于生产？**
   - **能转头的原因**：从各向同性的随机高斯噪声出发，去噪过程没有受到任何静态正面几何的先验锚定，Cross-Attention 能够自由引导潜变量收敛到 Prompt 指定的侧面视角；
   - **不能直接作为生产的原因**：
     1. **身份不可控**：纯 T2V 无法保证生成的侧面人物与特定参考数字人原图一致；
     2. **内生严重灰化与动态范围塌缩**：零参考潜变量下的 3D Causal VAE 因果时序反卷积，导致 Frame 1~7 逐帧上浮，黑位飙升至 41.41，动态范围剧烈塌缩 21.2%；
     3. **缺乏时序动力学**：当前纯 T2V 直接生成了一个静态侧脸，未能从第 0 帧正面平滑连贯地“旋转”到侧面。

---

## 五、最终生产边界与后续技术路线规划

1. **生产环境维持 C2 绝对封板**：
   `strength = 0.30`, 15 步迭代，全时序参考潜变量初始化（`use_full_sequence_reference = True`），严格用于 `AVATAR_STABLE_MICRO_MOTION` 口播微动作生产模式；
2. **大动作独立技术路线规划**：
   由于证明了纯文生视频无法仅凭文本完成“固定原图身份 + 真实正面到侧面转头”，未来的大动作数字人方案必须采用以下新范式之一：
   - **路线 A (ControlNet / DWPose 时序骨架驱动)**：提供真实人脸多角度连续旋转的骨架关节点，在去噪每一步显式控制几何朝向；
   - **路线 B (多视角关键帧插值架构)**：提供正面首帧与侧面末帧，在潜空间构建动态插值母本；
   - **路线 C (专业姿态迁移模型结合)**：先用 C2 生产高保真数字人视频，再通过 LivePortrait / Thin-Plate Spline 头部姿态迁移层执行角度旋转。
