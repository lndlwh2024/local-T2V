# 文生视频_原图插入 V1.0 配置信息与回滚手册

> **归档版本**：V1.0（对应 Git Tag `v1.2.2`）  
> **归档日期**：2026-09-11  
> **配置定位**：数字人 832x480 电影画幅 + 全时序潜空间渐进软锚定基线版本。在后续优化发生衰退时，可执行本手册完全恢复该基线画质。

---

## 一、 核心架构与模型组件

| 模块 | 模型全称 / 来源 | 精度 / 规格 | 运行设备 | 内存/显存驻留策略 |
| :--- | :--- | :--- | :--- | :--- |
| **主干生成模型** | Wan2.1-T2V-1.3B (Diffusion Transformer) | FP16 | GPU (CUDA) | 阶段2装载独占显存，去噪完成立即 `del` 并 `empty_cache` |
| **文本编码器** | Google UMT5-XXL | FP8 (Float8) | 系统内存 (CPU) | 纯 CPU 内存推理，完全不占用 4GB GPU 显存 |
| **分词器** | Wan2.1 AutoTokenizer | 官方分词词表 | CPU | 轻量驻留 |
| **调度器** | FlowMatchEulerDiscreteScheduler | 欧拉连续流匹配 | CPU / GPU | shift: 1.0, prediction_type: flow_prediction |
| **视频编解码器** | AutoencoderKLWan (3D Causal VAE) | **全精度 FP32** | GPU (CUDA) | 解耦至阶段4单独载入，开启 Tiling/Slicing，解码后释放 |

---

## 二、 核心生成参数与超参数清单

```json
{
  "version": "1.0",
  "pipeline_name": "WanT2VLowVramPipeline",
  "resolution": {
    "width": 832,
    "height": 480,
    "aspect_ratio": "16:9"
  },
  "temporal": {
    "num_frames": 9,
    "target_num_frames": 8,
    "fps": 8,
    "duration_sec": 1.0
  },
  "inference": {
    "num_inference_steps": 20,
    "strength": 0.20,
    "effective_steps": 4,
    "guidance_scale": 2.0,
    "seed": 42
  },
  "temporal_anchoring": {
    "enable_temporal_anchoring": true,
    "slice_0_weight": 1.0,
    "slice_1_weight": 0.85,
    "slice_2_weight": 0.80
  },
  "post_processing": {
    "temporal_contrast_restoration": true,
    "temporal_contrast_ratio": 0.85,
    "adaptive_edge_filter": true,
    "edge_filter_threshold": 8.0
  },
  "hardware_limits": {
    "vram_limit_gb": 3.6,
    "peak_vram_mb": 2952.19
  }
}
```

---

## 三、 提示词工程与输入定义

### 1. 正向提示词 (AVATAR_BASE_PROMPT)
```text
A photorealistic mature Asian man with a natural very short shaved buzz-cut hairstyle, neat natural hairline, groomed salt-and-pepper goatee, wearing a clean plain white crewneck t-shirt with a tiny red chest logo, standing in a modern high-tech broadcast studio with ambient soft studio lighting, 8k resolution, highly detailed realistic skin texture, cinematic quality. Action: The digital avatar gently and slowly opens his eyes with a calm, composed, dignified expression, looking steadily forward at the camera, maintaining exact lips and facial bone structure of the reference photo, natural subtle head breath motion, strictly no exaggerated smile.
```

### 2. 负向提示词 (NEGATIVE_PROMPT)
```text
色调艳丽，过曝，残影，模糊，扭曲，变形，多余的肢体，多余的手指，融化的物体，低分辨率，卡通，粗糙，光晕，白雾，distorted smile, fake smirk, exaggerated grin, unnatural grimace, open mouth, wide smile, deformed lips, mismatched facial expression
```

### 3. 参考原图
* 路径：`media/数字人大图正面.png`
* 预处理：832x480 中心人脸构图缩放裁剪，标准化至 $[-1.0, 1.0]$，经 VAE 编码为 $z_{\text{ref0}}$。

---

## 四、 一键回滚执行命令

若后续算法发生衰退，可在项目根目录下执行以下命令，严格复现 V1.0 的视频质量：

```bash
.venv\Scripts\python run_avatar_pipeline.py --shot shot_01 --width 832 --height 480 --steps 20 --strength 0.20 --seed 42 --force
```
