# 文生视频数字人原图插入技术方案 V2.11 配置信息 (阶段 D2.3: 5秒 / 4fps / 20帧 纯 T2V 大动作能力诊断与长时序时空建模能力判定)

## 一、版本演进说明

- **V1.0 基线版**：首帧单切片注入 + 软锚定（85%/80%）+ 20 步。重影与水波纹严重。
- **V2.0 ~ V2.3 隔离验证版**：彻底移除时序软锚定（根除波纹），关闭后处理发现内生发灰，通过纯 VAE 隔离测试确诊 VAE 高保真（衰减仅 0.16%）。
- **V2.4 (B3 黄金基线)**：接入全时序参考潜变量一次性因果编码（$T=3$），黑位完全零上浮（$\Delta = -0.80$），动态范围零衰减（$\Delta = -0.32$），彻底根治泛白发雾。
- **V2.5 (C1 验证版)**：`strength = 0.25`（12 步去噪），微动态初步激活，零波纹零发雾。
- **V2.6 (阶段 C 生产模式甜点搜索裁定版)**：
  - 完成阶段 C 微动作梯度探索（B3: 0.20 -> C1: 0.25 -> C2: 0.30 -> C3: 0.35）；
  - 正式将 **C2 (`strength = 0.30`) 确立并封板为生产黄金甜点版**（Production Sweet Spot / Golden Baseline）；
  - 正式将 **C3 (`strength = 0.35`) 归档为上边界压力参考版**（Experimental Upper Boundary），明确不作为生产默认参数。
- **V2.7 ~ V2.8 (阶段 D1 / D1.5 动作响应能力审计版)**：
  - 在全序列参考架构下进行 strength 0.30 / 0.35 / 0.40 压力测试；
  - 确认在静态全序列参考下，大转头动作完全被抑制（位移仅 1.47~1.65，转角为 0°）。
- **V2.9 (阶段 D2-Control 纯 T2V 对照版)**：
  - 关闭全序列参考（`initial_latents_source = RANDOM_NOISE`），在 CFG=2.0 下生成了一个静态侧脸，证明参考潜变量确实压制了侧转，但时序维度仍未发生连续转头过渡。
- **V2.10 (阶段 D2.1 提高 CFG 验证与判定 B 裁定版)**：
  - 将 `guidance_scale` 提高至 6.0，8 帧依然呈现近乎完全静态的正面像；
  - 正式确立判定 B：禁止继续盲目调高 CFG。
- **V2.11 (阶段 D2.3 长时序大动作能力诊断版，本版本)**：
  - 彻底摆脱短时序约束，拓展至 **5.0 秒 / 4 fps / 20 帧**（底层 21 帧，潜空间 6 个 temporal slice）；
  - 保持纯 T2V（纯随机高斯噪声初始化、零参考图、零锚定、零原图替换、零后处理），CFG=6.0，50 步去噪；
  - 产出完整 20 帧无损帧、4fps 预览图、20 帧人脸条带、0/10/19 三联对比图与 0/5/10/15/19 五联关键姿态演化图；
  - 对基础模型大动作时序能力做出最终裁定。

---

## 二、实验 D2.3 核心配置与实测指标

### 1. 核心控制变量
- **实验模式**：纯 T2V 对照模式 (`use_full_sequence_reference = False`)
- **初始潜变量来源**：`initial_latents_source = RANDOM_NOISE` (纯高斯白噪声)
- **视频时长与帧率**：`duration = 5.0s`, `fps = 4`
- **底层生成帧数**：`num_frames = 21` (Wan 3D VAE 因果卷积对齐公式 $4n+1$, $n=5$)
- **目标输出帧数**：`target_output_frames = 20` (精准切除末尾 1 帧冗余 padding)
- **去噪步数**：`num_inference_steps = 50`, `actual_iteration_count = 50`
- **引导权重**：`guidance_scale = 6.0`
- **分辨率与硬件**：832x480 @ NVIDIA Quadro T1000 4GB (DiT FP16 + UMT5 FP8 + VAE FP32)
- **随机数种子**：`seed = 42`
- **输出目录**：`run_D2_3_5s_4fps_20f_pure_t2v/`

### 2. 运行时性能与诊断数据
- **Transformer 去噪总耗时**：12,518.87 秒 (~3.48 小时)
- **流水线端到端耗时**：12,761.74 秒 (~3.54 小时)
- **峰值物理显存占用**：**3,174.06 MB** (严格锁定在 3.6GB 安全红线内，全程零 OOM)
- **Prompt SHA256 哈希值**：`c42e9481876e882f3e001b50765e246289e12a8134b9b3778ecae8b5e864a650`
- **Text Embedding 校验和**：`b6a7de094440862fb71583a7040d52adc06e9fc5b4e2b18fbb8967b1ea37d1bf`
- **实际 Timesteps 采样轨迹**：
  - 前 5 步：`[1000.0, 979.6122, 959.2245, 938.8367, 918.449]`
  - 后 5 步：`[82.551, 62.1633, 41.7755, 21.3878, 1.0]`

### 3. 潜空间 6 个 Temporal Slice 去噪后统计审计
| 切片序号 | 对应真实帧区间 | 均值 (mean) | 标准差 (std) | 最小值 (min) | 最大值 (max) |
| :---: | :---: | :---: | :---: | :---: | :---: |
| **Slice 0** | Frame 0 | -0.0694 | 0.7273 | -2.4570 | +2.3359 |
| **Slice 1** | Frame 1 ~ 4 | -0.1260 | 1.1551 | -2.6113 | +2.6289 |
| **Slice 2** | Frame 5 ~ 8 | -0.1245 | 1.3420 | -2.9512 | +2.9199 |
| **Slice 3** | Frame 9 ~ 12 | -0.1196 | 1.4276 | -3.2832 | +3.0684 |
| **Slice 4** | Frame 13 ~ 16 | -0.0968 | 1.4282 | -3.2227 | +3.1465 |
| **Slice 5** | Frame 17 ~ 20 | -0.0943 | 1.4254 | -3.3418 | +3.1797 |

---

## 三、5 维核心诊断最终裁定

渲染完成后，基于导出的 20 帧全画幅序列、`contact_sheet.png`、`f0_vs_f10_vs_f19_compare.png` 与 `temporal_keypose_sheet.png` 进行客观严谨的视觉与动力学裁定：

### 1. Frame 0 / 10 / 19 是否形成明显不同姿态？
- **最终裁定：【否】**。
- **客观事实**：Frame 0、Frame 10、Frame 19 的主体身体朝向与面部朝向角度完全未呈现分阶段转变。人物从第 0 帧起就已经以固定角度侧立（左人朝右侧转约 60°，右人朝左侧偏约 40°）。在历经 2.5 秒（第 10 帧）和 4.75 秒（第 19 帧）后，人物的骨骼姿态与朝向角度完全被“冻结”，仅存在局部嘴唇轻微张合与微弱噪点晃动，没有姿态阶段差异。

### 2. 是否存在“正面 $\to$ 半转 $\to$ 侧转”的清晰三阶段结构？
- **最终裁定：【完全不存在】**。
- **客观事实**：整整 20 帧（5.0 秒）中，没有任何一帧呈现“初始正面直视镜头”的形态。时间轴上根本没有经历由正面起步、中间转向、末尾定格在侧面的动力学演变过程。

### 3. 动作是连续演化，还是“静态姿态重复”？
- **最终裁定：【依然是静态姿态重复】**。
- **客观事实**：动作演化仅局限于局部高频纹理扰动、微弱呼吸起伏和嘴唇边缘的微颤（亚像素级微动作），骨骼级、角度级的大动作在 5 秒的长时序维度上完全停滞。

### 4. 是否仍然主要表现为“侧脸复印机”或“静态人像视频化”？
- **最终裁定：【是，且诱发了空间多主体退化】**。
- **客观事实**：模型不仅表现为“静态人像视频化”，更由于 832x480 宽屏画幅与文本中同时出现的“facing directly”和“40-degree left-facing”，在无法理解时序先后关系的情况下，直接在空间维度上退化生成了两个侧向对望的人物（左右双人构图），整段视频实质上是“双人静态像的轻微抖动视频化”。

### 5. 若仍无明显动作，是否可以强烈怀疑：当前 Wan2.1-T2V-1.3B + 当前实现对纯文本大动作的时序建模能力不足？
- **最终裁定：【完全确认，纯文本大动作时序建模能力严重不足】**。
- **科学定论**：实验 D2.3 彻底剔除了所有外部干扰（消除了 1 秒过短的采样限制，排除了 CFG 偏低的影响，移除了参考图潜变量的时序刚性阻尼，关闭了首帧锚定）。在 5 秒长时序与 20 帧的高维潜在空间中，Wan2.1-T2V-1.3B 基础模型依然无法将文本描述的时间因果演化映射为时序潜变量的连续旋转，而是将文本全局特征塌缩为单帧静态先验。这充分证明：**Wan2.1-T2V-1.3B 基础模型的纯文本时序大动作建模能力极其有限。**

---

## 四、对数字人生产架构的指导结论

1. **确立生产模式的科学性**：
   阶段 C 封板的 **C2 正式生产模式 (`AVATAR_STABLE_MICRO_MOTION`, strength=0.30)** 是 100% 正确且唯一可行的技术路线！该模式通过全时序参考潜变量因果初始化，以极低代价锁定了数字人的超高画质、真实五官骨相与零泛白零波纹，完美胜任播报、眨眼与呼吸等口播微动作。
2. **界定模型物理边界**：
   严禁尝试通过 Prompt 引导纯文本大动作（如大幅转头、身体旋转等），纯文本大动作在当前轻量级基础模型下无法成立；
3. **大动作扩展的技术路径明确**：
   若后续业务确需数字人大动作，必须引入**显式时序条件控制器**（如 ControlNet OpenPose 姿态骨架序列、DensePose 密集表面映射或 LivePortrait/EchoMimic 姿态潜变量驱动），而非依赖纯文本提示词。
