# 文生视频数字人原图插入技术方案 V2.10 配置信息 (阶段 D2.1: 纯 T2V 提高 CFG 验证与判定 B 裁定)

## 一、版本演进说明

- **V1.0 基线版**：首帧单切片注入 + 软锚定（85%/80%）+ 20 步。重影与水波纹严重。
- **V2.0 ~ V2.3 隔离验证版**：彻底移除时序软锚定（根除波纹），关闭后处理发现内生发灰，通过纯 VAE 隔离测试确诊 VAE 高保真（衰减仅 0.16%）。
- **V2.4 (B3 黄金基线)**：接入全时序参考潜变量一次性因果编码（$T=3$），黑位完全零上浮（$\Delta = -0.80$），动态范围零衰减（$\Delta = -0.32$），彻底根治泛白发雾。
- **V2.5 (C1 验证版)**：`strength = 0.25`（12 步去噪），微动态初步激活，零波纹零发雾。
- **V2.6 (阶段 C 生产模式甜点搜索裁定版)**：
  - 完成阶段 C 微动作梯度探索（B3: 0.20 -> C1: 0.25 -> C2: 0.30 -> C3: 0.35）；
  - 正式将 **C2 (`strength = 0.30`) 确立并封板为生产黄金甜点版**（Production Sweet Spot / Golden Baseline）；
  - 正式将 **C3 (`strength = 0.35`) 归档为上边界压力参考版**（Experimental Upper Boundary），明确不作为生产默认参数。
- **V2.7 ~ V2.8 (阶段 D1 / D1.5 动作响应能力审计版)**：
  - 在全序列参考架构下进行 strength 0.30 / 0.35 / 0.40 压力测试；
  - 确认在静态全序列参考下，大转头动作完全被抑制（位移仅 1.47~1.65，转角为 0°）。
- **V2.9 (阶段 D2-Control 纯 T2V 对照版)**：
  - 关闭全序列参考（`initial_latents_source = RANDOM_NOISE`），在 CFG=2.0 下生成了一个静态侧脸，证明参考潜变量确实压制了侧转，但时序维度仍未发生连续转头过渡。
- **V2.10 (阶段 D2.1 提高 CFG 验证与判定 B 裁定版，本版本)**：
  - 将 `guidance_scale` 由 2.0 提升至 6.0，并注入严格的三阶段时序描述 Prompt；
  - 实测表明：**CFG=6 下人物在 8 帧中依然保持近乎完全静态的正面姿态，完全未发生“正面 -> 转头 -> 侧面”的时序动力学**；
  - **判定 B 正式确立**：不要继续盲目调高 CFG，明确转入下一阶段针对底层时序条件传递与 Wan1.3B 动力学特性的深度排查。

---

## 二、实验 D2.1 核心配置与实测数据

### 1. 控制变量与唯一修改
- **唯一修改项**：`guidance_scale (CFG)` 由 `2.0` 提高至 **`6.0`**
- **潜变量初始化模式**：`use_full_sequence_reference = False`
- **初始潜变量来源**：`initial_latents_source = RANDOM_NOISE` (纯高斯白噪声，零参考图输入)
- **时序锚定**：`enable_temporal_anchoring = False` (`anchor_weight = 0.0`)
- **后处理状态**：`enable_post_processing = False` (纯净 Raw Decoded 帧导出)
- **去噪步数**：`num_inference_steps = 50`, `actual_iteration_count = 50`
- **模型架构**：Wan2.1-T2V-1.3B (Transformer FP16 + UMT5-XXL FP8 + FP32 3D Causal VAE)
- **画幅规格**：832x480 @ 8fps, 9 帧生成截取前 8 帧输出 (1.0 秒)
- **Seed**：42

### 2. 文本特征哈希校验对照 (证明文本条件已实质改变)
- **C2 微动作基准 Prompt Hash (SHA256)**：
  `b139ac390ce2d7ee38032d125392da0bfdf69245c67c15fb7b598b7ad455ba6c`
- **D2.1 三阶段时序大动作 Prompt Hash (SHA256)**：
  `72e420d709750efb6650f329a3523dcbf57ad727b8336921169c0e8e1d061b2e`
- **Prompt Embedding Checksum (SHA256)**：
  `3af6a9e753c604f82e33513cbb088ac30f32abe8132dd7d93be182ffdb15afdf`
- **二者对比**：`is_prompt_different_from_c2 = True`（文本先验完全不同，哈希严格隔离）。

---

## 三、横向全景实验指标对比表

> **标准光度学亮度公式**：$Y = 0.299 \times R + 0.587 \times G + 0.114 \times B$

| 实验版本 | 潜变量初始来源 | CFG | 去噪步数 | 实测人脸朝向与动作形态 | Frame 1 黑位 | Frame 7 黑位 | 黑位漂移 (Delta) | 动态范围衰减率 |
| :--- | :--- | :---: | :---: | :--- | :---: | :---: | :---: | :---: |
| **C2 (生产甜点)** | 9帧静态原图因果母本 | 2.0 | 15 步 | **纯正面，微表情生动流转** | 0.98 | 1.84 | +0.85 | 0.86% |
| **D1 (大动作压力)** | 9帧静态原图因果母本 | 2.0 | 15 步 | **纯正面，微表情，大动作被锁死** | 3.33 | 4.63 | +1.30 | 1.05% |
| **D2-Control (纯T2V)** | 纯随机噪声 (RANDOM_NOISE) | 2.0 | 50 步 | **静态侧转姿态 (~45°~60°)，无动态过程** | 25.64 | 41.41 | +15.77 | 21.20% |
| **D2.1 (提高CFG)** | **纯随机噪声 (RANDOM_NOISE)** | **6.0** | **50 步** | **纯正面，8帧近乎完全静态，无转头过程** | **2.15** | **26.78** | **+24.63** | **21.09%** |

---

## 四、最终裁定与技术判定（判定 B 正式确立）

### 1. 判定 B 明确结论
> **“在 guidance_scale = 6.0 且使用显式三阶段时序动作描述的情况下，生成的视频依然呈现为 8 帧近乎完全静态的正面人像，未发生由正面转至侧面的连续动力学过程。判定 B 正式确立：禁止继续盲目调高 CFG。”**

### 2. 下一阶段深度排查路线规划
根据判定 B 规则，后续排查重点严格锁定为以下三个底层工程方向：
1. **Prompt Embedding 跨层传递审计**：检查 UMT5-XXL 提取的 512 长度文本 Token 是否完整穿透进入 Wan 1.3B 的 Cross-Attention 层；
2. **Temporal Conditioning 机制审计**：排查当前 diffusers WanPipeline 实现中，是否缺少时序条件嵌入（如 frame_rate / motion_bucket_id / temporal_rope），导致 DiT 在时间维度注意力层仅执行均值池化而丧失动力学引导；
3. **Wan2.1-T2V-1.3B 物理尺度上限认知**：排查 Wan 1.3B 轻量级模型是否本身在短时（1.0 秒 / 8 帧）下存在天然的动力学惰性（倾向于将整句 Prompt 塌缩为一个静态画面的内容描述，而非生成动画过渡）。
