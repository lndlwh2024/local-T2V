# 文生视频_原图插入 V2.1 配置信息与回滚手册

> **归档版本**：V2.1（对应 Git Tag `v1.2.5` / 实验 A2-1 采样密度升级版）  
> **归档日期**：2026-09-11  
> **配置定位**：数字人 832x480 电影画幅 + 首帧母本硬替换 + 关闭潜空间时序软锚定 + **去噪步数提升至 50 步（实际迭代 10 步）**。相比 V2.0 (20步/4迭代)，微表情与高频五官（眼裂、泪沟、双眼皮折痕与胡茬）去噪更充分，神态沉稳，整整 8 帧完全杜绝表情漂移。

---

## 一、 与 V2.0 核心变更对比

| 对比维度 | V2.0 基础版本 (v1.2.3 / 实验 A1) | **V2.1 优化升级配置 (v1.2.5 / 实验 A2-1)** | 设计原因与物理增益 |
| :--- | :--- | :--- | :--- |
| **配置步数** | `num_inference_steps = 20` | **`num_inference_steps = 50`** | 提高时间流匹配采样密度，细化速度向量场积分步长 |
| **去噪强度** | `strength = 0.20` | **`strength = 0.20`** | 严格保持 80% 原图骨相先验不变 |
| **实际迭代次数** | `actual_iteration_count = 4` | **`actual_iteration_count = 10`** | 去噪步数增加 1.5 倍，欧拉流匹配步长 $dt$ 从约 52 缩小至约 20 |
| **细节保全度** | 局部存在轻微平滑模糊 | **五官细纹、睫毛与胡茬清晰度明显增强** | 小步长积分使得微表情高频特征在尾段得到充分去噪 |
| **神态稳定性** | 良好 | **极佳（8帧从容端正、完全无表情拉扯）** | 10 步均匀迭代让 DiT 动作演化更加线性平稳 |
| **去噪耗时** | 约 399 秒 (~6.6 分钟) | **约 997 秒 (~16.6 分钟)** | 以充裕算力换取极致面部五官稳定性 |
| **物理峰值显存** | 2952.19 MB | **2952.19 MB (完全一致)** | 步数增加仅增加时间循环，张量维度不变，零额外显存占用 |

---

## 二、 核心架构与模型组件

| 模块 | 模型全称 / 来源 | 精度 / 规格 | 运行设备 | 内存/显存驻留策略 |
| :--- | :--- | :--- | :--- | :--- |
| **主干生成模型** | Wan2.1-T2V-1.3B (Diffusion Transformer) | FP16 | GPU (CUDA) | 独占显存，峰值 2952.19MB，去噪完成后彻底释放 |
| **文本编码器** | Google UMT5-XXL | FP8 (Float8) | 系统内存 (CPU) | 纯 CPU 内存推理，完全不占用 4GB GPU 显存 |
| **分词器** | Wan2.1 AutoTokenizer | 官方分词词表 | CPU | 轻量驻留 |
| **调度器** | FlowMatchEulerDiscreteScheduler | 欧拉连续流匹配 | CPU / GPU | shift: 1.0, prediction_type: flow_prediction |
| **视频编解码器** | AutoencoderKLWan (3D Causal VAE) | **全精度 FP32** | GPU (CUDA) | 解耦至阶段4单独载入，开启 Tiling/Slicing，解码后释放 |

---

## 三、 核心生成参数与超参数清单

```json
{
  "version": "2.1",
  "pipeline_name": "WanT2VLowVramPipeline",
  "resolution": {
    "width": 832,
    "height": 480,
    "aspect_ratio": "16:9"
  },
  "temporal": {
    "num_frames": 9,
    "target_num_frames": 8,
    "fps": 8,
    "duration_sec": 1.0
  },
  "inference": {
    "num_inference_steps": 50,
    "strength": 0.20,
    "effective_steps": 10,
    "guidance_scale": 2.0,
    "seed": 42
  },
  "temporal_anchoring": {
    "enable_temporal_anchoring": false,
    "slice_0_weight": 1.0,
    "slice_1_weight": 0.0,
    "slice_2_weight": 0.0
  },
  "post_processing": {
    "temporal_contrast_restoration": true,
    "temporal_contrast_ratio": 0.85,
    "adaptive_edge_filter": true,
    "edge_filter_threshold": 8.0
  },
  "hardware_limits": {
    "vram_limit_gb": 3.6,
    "peak_vram_mb": 2952.19
  }
}
```

---

## 四、 14 项底层运行时诊断数据快照

```text
1. scheduler 实际类名: FlowMatchEulerDiscreteScheduler
2. scheduler.config 完整关键参数:
   - shift: 1.0
   - prediction_type: flow_prediction
   - num_train_timesteps: 1000
3. num_inference_steps 配置值: 50
4. 实际执行的 scheduler timestep 数量: len(timesteps) = 10
5. 实际 timesteps 前3个与后3个值:
   - 前3个值: [184.4898, 164.1021, 143.7143]
   - 后3个值: [41.7755, 21.3878, 1.0]
6. sigmas 取值:
   - 前5个: [1.0, 0.9796, 0.9592, 0.9388, 0.9184]
   - 后5个: [0.0622, 0.0418, 0.0214, 0.001, 0.0]
7. guidance_scale 实际值: 2.0
8. strength 实际值: 0.20
9. VAE encode 调用总次数: 1 次 (首帧离线编码)
10. VAE decode 调用总次数: 1 次 (分镜批量解码)
11. 每个 temporal slice 实际 frame 范围:
    - Slice 0: Frame 0 (第0帧)
    - Slice 1: Frame 1 ~ Frame 4 (第1~4帧)
    - Slice 2: Frame 5 ~ Frame 8 (第5~8帧)
12. 每个 slice 使用的 anchor 权重:
    - Slice 0: 1.0 (原图直接替换)
    - Slice 1: 0.0 (anchor_weight = 0，关闭锚定)
    - Slice 2: 0.0 (anchor_weight = 0，关闭锚定)
13. 是否存在 generated frame -> VAE encode 参与后续生成: 否 (False)
14. 是否存在 clean reference 直接与当前 noisy latent 线性混合: 否 (False)
```

---

## 五、 一键回滚 / 复现执行命令

若后续实验探索出现效果劣化，可在项目根目录下执行以下命令，严格 100% 还原当前 V2.1 最优画质与全部 8 帧输出：

```bash
.venv\Scripts\python run_avatar_pipeline.py --shot shot_01 --width 832 --height 480 --steps 50 --strength 0.20 --seed 42 --no-temporal-anchor --output-dir run_A2_1_steps50_strength020 --force
```
