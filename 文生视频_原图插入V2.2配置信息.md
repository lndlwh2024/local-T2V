# 文生视频数字人原图插入技术方案 V2.2 配置信息 (实验 B1 原始解码归档版)

## 一、版本演进说明

- **V1.0 基线版**：采用首帧原图潜变量条件注入 + 软锚定（Slice 1 85%，Slice 2 80%）+ 20 步采样 + 后处理（时序对比度拉伸与自适应去条纹滤波）。存在明显水波纹与重影。
- **V2.0 实验 A1 验证版**：彻底关闭 Progressive Temporal Identity Anchoring（Frame 1~8 `anchor_weight = 0`），成功根除了重影与水波纹伪影。
- **V2.1 实验 A2-1 验证版**：在 A1 基础上保持 `anchor_weight = 0`，将采样步数由 20 步提升至 50 步（实际去噪迭代由 4 次增至 10 次），五官清晰度与发丝细节显著跃升。
- **V2.2 实验 B1 诊断版（本版本）**：
  - **唯一控制变量**：彻底关闭视频后处理管线（`enable_post_processing = False`，跳过时序对比度校准与垂直保边滤波），严格禁止任何逐帧归一化，仅输出 VAE 原始解码张量经固定静态线性变换 `(raw_frames * 255.0).round().astype(np.uint8)` 的原始帧（Raw Decoded Frames）。
  - **核心科学结论**：证实“逐帧泛白 / 虚化 / 发雾”**100% 根源于主模型 / 3D-VAE 的内生时序动态范围衰减**，与后处理无关。在无任何后处理的情况下，原始解码帧从 Frame 2 起黑位由 0.0 漂移至 25.17，高光从 251.59 塌缩至 219.03，动态范围区间萎缩 23%~30%，方差收缩导致画面视觉泛白发雾。

---

## 二、完整生成与后处理参数配置表

```json
{
  "version": "2.2",
  "experiment_id": "run_B1_raw_decode",
  "pipeline_name": "WanVideoToVideoPipeline",
  "model_architecture": {
    "diffusion_transformer": "Wan2.1-T2V-1.3B (FP16, ~2.7GB GPU VRAM)",
    "text_encoder": "google/umt5-xxl (FP8, Pure CPU Offload, ~11GB System RAM)",
    "vae": "Wan2.1_VAE (FP32 Causal 3D-VAE, Slicing & Tiling Enabled)"
  },
  "generation_parameters": {
    "width": 832,
    "height": 480,
    "fps": 8,
    "num_frames": 9,
    "target_num_frames": 8,
    "configured_num_inference_steps": 50,
    "strength": 0.20,
    "actual_iteration_count": 10,
    "guidance_scale": 2.0,
    "seed": 42,
    "scheduler": {
      "class": "FlowMatchEulerDiscreteScheduler",
      "shift": 1.0,
      "prediction_type": "flow_prediction",
      "num_train_timesteps": 1000
    }
  },
  "temporal_anchoring": {
    "enable_temporal_anchoring": false,
    "frame_0": {
      "anchor_weight": 1.0,
      "replacement_mode": "direct_reference_latent_overwrite"
    },
    "slice_1_frames_1_to_4": {
      "anchor_weight": 0.0,
      "status": "bypassed"
    },
    "slice_2_frames_5_to_8": {
      "anchor_weight": 0.0,
      "status": "bypassed"
    }
  },
  "post_processing_pipeline": {
    "enable_post_processing": false,
    "temporal_contrast_restoration_executed": false,
    "adaptive_destripe_filter_executed": false,
    "frame_level_normalization_exists": false,
    "output_pipeline_note": "当前导出的是 raw decoded frames，无后处理",
    "residual_path_note": "除 VAE 原始解码与固定线性变换 (np.clip(val * 255.0, 0, 255).astype(np.uint8)) 外，不存在任何隐式色彩或滤波处理。"
  }
}
```

---

## 三、实验 B1 光度学帧亮度实测数据 (Luma Stats)

> **光度学标准亮度公式**：$Y = 0.299 \times R + 0.587 \times G + 0.114 \times B$

| 帧序号 | 帧类型 | 亮度均值 (Mean Luma) | 亮度标准差 (Std Luma) | 最低灰阶 (Min) | 最高灰阶 (Max) | 动态范围 (Dynamic Range) | 视觉特征 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **Frame 0** | 基准参考图 | **141.28** | **54.88** | **0.00** | **251.59** | **251.59** | 纯净透亮，深邃黑位，五官立体 |
| **Frame 1** | Raw Decode | 135.72 | 52.11 | 4.54 | 243.59 | 239.05 | 接近基准，轻度衰减 |
| **Frame 2** | Raw Decode | 126.37 | 45.12 | 18.12 | 229.94 | 211.82 | **开始显著发灰、泛白，黑位上漂** |
| **Frame 3** | Raw Decode | 125.54 | 44.57 | 18.90 | 226.82 | 207.92 | 持续发灰，双眼皮阴影变浅 |
| **Frame 4** | Raw Decode | 125.39 | 42.17 | 25.17 | 221.99 | 196.82 | 灰雾显著，暗部抬升达 25 阶灰度 |
| **Frame 5** | Raw Decode | 125.82 | 43.10 | 22.75 | 223.74 | 200.99 | 灰雾持续 |
| **Frame 6** | Raw Decode | 122.57 | 41.06 | 23.12 | 220.55 | 197.43 | 灰度收缩达到峰值 |
| **Frame 7** | Raw Decode | 121.09 | 40.81 | 24.25 | 219.03 | 194.78 | 动态范围萎缩 22.6%，画面起雾 |

---

## 四、核心科学机理诊断与后续解决路径

1. **时序因果动态范围收缩（Temporal Dynamic Range Shrinkage）**：
   - Wan 3D-VAE 使用因果时序卷积结构（Causal 3D Conv）。在因果展开过程中，后续时间切片（Chunk > 0）的潜变量特征激活向均值收敛，导致反卷积还原像素时，色彩动态范围由基准帧的 $[0, 252]$ 剧烈压缩至 $[25, 219]$。
2. **后处理的必要性确认**：
   - 实验 B1 决定性地证明：**不能裸露输出原始解码结果**，必须通过时序动态范围恢复技术将收缩的黑位（25 -> 0）与高光（219 -> 250）线性校准回归。
3. **后续优化方向（为下一阶段指明路径）**：
   - 恢复并进一步升级时序校准算法（采用更加精准平滑的非线性直方图锚定或保真曲线），在消除灰雾的同时，杜绝极端像素截断。
